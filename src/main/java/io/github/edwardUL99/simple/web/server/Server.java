package io.github.edwardUL99.simple.web.server;

/**
 * Represents a Server that uses the sockets package, parsing and requests to listem for http requests and respond to them
 */
public interface Server {
    void listen();
}
